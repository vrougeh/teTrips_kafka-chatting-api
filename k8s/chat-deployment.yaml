apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  labels:
    app: chat-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      nodeSelector:
        kubernetes.io/hostname: tetrips-msa-np-w-5i0a # 다른 노드 이름으로 변경 필요
      containers:
        - name: chat-service
          image: honggyun/tetrips-chat-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_DATA_MONGODB_URI
              value: mongodb://chatuser:chatpassword@mongo-service/chatdb
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: PLAINTEXT://kafka-cluster-0.kafka-cluster:9092,kafka-cluster-1.kafka-cluster:9092,kafka-cluster-2.kafka-cluster:9092,afka-cluster-3.kafka-cluster:9092,kafka-cluster-4.kafka-cluster:9092,kafka-cluster-5.kafka-cluster:9092,afka-cluster-6.kafka-cluster:9092,kafka-cluster-7.kafka-cluster:9092,kafka-cluster-8.kafka-cluster:9092
